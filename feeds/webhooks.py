from reader import Entry


def send_entry_to_webhook(entry: Entry, webhook_url: str) -> str:
    """Send an entry to a webhook."""
    # TODO(TheLovinator): Implement this
    return "Sent entry to webhook"
